"""
FastAPI dependencies for API keys.

Currently empty - no auth required (matches Go implementation).
Future: Add authentication when gRPC auth server is implemented.
"""

# No dependencies needed yet
# API key endpoints are currently public (matches Go backend)
